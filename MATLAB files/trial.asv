x=val;
y=awgn(x,5);
subplot(2,1,1)
plot(x);
subplot(2,1,2)
plot(y);
mse=sum((x-y).^2)/length(x);
disp(mse);
w=50/(250/2);
bw=w;
[num,den]=iirnotch(w,bw);
filtered_y=filter(num,den,y);
mse=sum(